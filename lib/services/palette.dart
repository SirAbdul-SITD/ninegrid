import 'package:flutter/material.dart';

/// Cool graphite-and-jade theme: neutral dark grid, jade-green selection
/// and givens contrast -- distinct from every other title in this catalog,
/// deliberately understated for the most classic puzzle in the set.
class Palette {
  static const void_ = Color(0xFF13181B);
  static const panel = Color(0xFF1B2225);
  static const raised = Color(0xFF252E32);

  static const cellFill = Color(0xFF1E2629);
  static const cellGiven = Color(0xFF232D30);
  static const cellSel = Color(0xFF2B4640);
  static const cellPeer = Color(0xFF212B2C);

  static const ink = Color(0xFFEFF4F2);
  static const givenInk = Color(0xFFB9C8C3);
  static const jade = Color(0xFF5BAE8C);
  static const haze = Color(0xFF7E8D89);
  static const line = Color(0xFF2C3639);
  static const boxLine = Color(0xFFA9BAB5);

  static const coral = Color(0xFFD1685A);

  static const tierColors = {
    'easy': Color(0xFF6FA37B),
    'medium': Color(0xFFC9A24B),
    'hard': Color(0xFFD1685A),
  };
}
